<%= form_for @shift_job, :html => { :class => 'form-horizontal' } do |f| %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> Shift Job</legend>

    <div class="control-group">
      <%= f.label :shift_id, :class => 'control-label' %>
      <div class="controls">
        <%= f.number_field :shift_id, :class => 'number_field' %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :job_id, :class => 'control-label' %>
      <div class="controls">
        <%= f.number_field :job_id, :class => 'number_field' %>
      </div>
    </div>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', shift_jobs_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
